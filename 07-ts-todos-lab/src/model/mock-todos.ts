import { TodoCreateDTO } from "./todos";

const MOCK_TODOS = [
    new TodoCreateDTO('Create TODO App using create-react-app scripts'),
    new TodoCreateDTO('Create TodoApp component'),
    new TodoCreateDTO('Create TodoList component'),
    new TodoCreateDTO('Create TodoItem component'),
    new TodoCreateDTO('Create TodoInput component'),
    new TodoCreateDTO('Debug the application'),
    new TodoCreateDTO('Add TODO filtering by status'),
    new TodoCreateDTO('Improve styling')
];

export default MOCK_TODOS;